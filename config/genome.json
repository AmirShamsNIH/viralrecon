{
    "_comment": [
        "Template only: `viralrecon build` writes the real genome.json passed to",
        "`viralrecon run --genome`, and only references.target is read."
    ],
    "references": {
        "target": {
            "BIOWULF": {}
        }
    }
}
